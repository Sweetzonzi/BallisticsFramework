# 终点弹道协议（Terminal Ballistics Protocol）技术设计文档

## 一、项目定位与设计目标

本项目为 Minecraft NeoForge/Forge 模组生态定义一套"终点弹道伤害协议层（Terminal Ballistics Damage Protocol Layer）"。其核心定位是一个可供多个枪械、载具、护甲模组共同依赖的 **lib 级兼容协议**，而非一个独立运行的 damage overhaul mod。

协议的终极目标是解决以下问题：在 Minecraft 原生伤害系统中，`DamageSource` 是被设计为不可变的、registry 化的"伤害类型标签"，其职责仅限于描述伤害的种类和来源实体。对于高复杂度场景——枪炮弹道、穿甲深度、命中角度、入射法线、装甲等效厚度、模块化部件伤害等——这些信息是**一次命中过程中临时产生的高维上下文**，它们既不适合塞入 `DamageSource`（那是类型描述而非数据容器），也不适合持久存储在实体 Capability 中（它们只在该次伤害调用链内有意义）。在没有协议的情况下，不同武器模组和装甲模组只能各自发明私有的 ad-hoc 方案，互不兼容。

协议的设计原则如下：

1. **不替代原版，而是包裹原版**：协议绝不绕开 `Entity#hurt`/`LivingEntity#hurt`、绝不直接 `setHealth`、绝不重新实现完整的 combat pipeline。对于未声明协议感知的普通实体，伤害完全走原版流程，协议层透明不参与。
2. **对声明协议感知的目标接管伤害入口**：当一个实体实现了 `TBHurtTarget` 接口，它便声明了"我自行处理穿甲判定和伤害后果"。此时协议层在完成穿甲协商后直接将最终伤害值交给该目标，由目标自行决定是调用 `super.hurt()` 走原版管线，还是完全自定义处理。
3. **上下文通过 ThreadLocal 隐式传递，但 API 完全封装**：由于 vanilla 的 `hurt` 方法签名是固定的，无法显式传递额外参数，协议层使用 ThreadLocal 栈在调用链内传播上下文。但所有 push/pop/try/finally 细节完全内嵌于 `TBDamageApi.hurt()` 内部，协议使用者永远不需要直接接触 ThreadLocal。
4. **双层设计——简易模式与精密模式并重**：协议的默认行为基于离散的 `ArmorLevel` 穿甲/护甲等级体系，简单模组只需实现一个方法即可获得完整的穿甲判定（HD2 风格）。需要精确毫米级判定的模组可覆写全部管线方法深化至精细纯数值模型。穿深的角度效应等归弹头/武器侧计算，协议不默认修正。
5. **协议外伤害有限兼容**：原版生物攻击、TNT 爆炸、岩浆、虚空、指令等"非协议来源"的伤害，如果命中了一个 `TBHurtTarget`，协议层会自动将它转换为一个低信息量的协议上下文，然后走完整穿甲判定管线。这通过 Mixin 注入 `Entity#hurt` 和 `LivingEntity#hurt` 实现。

## 二、软件包与文件架构

### 2.1 总体布局

```
io.github.sweetzonzi.terminal_ballistics/
│
├── TerminalBallistics.java              ← @Mod 主类，入口
│
├── api/                                 ← 公共 API 面，供其他 mod 编译依赖
│   ├── ArmorLevel.java                  ← 穿甲/护甲等级枚举 + 映射工具 + 离散判定
│   ├── TBDamageApi.java                 ← 唯一对外入口（hurt、hasContextFor、getContextFor）
│   ├── TBDamageContext.java             ← 上下文值对象（7 字段 + getPenetrationLevel）
│   ├── TBDamageContextBuilder.java      ← TBDamageContext 的 Builder
│   ├── TBDamageExtensions.java          ← 类型安全扩展容器 + 预定义扩展 key 常量
│   ├── TBDamageExtensionKey.java        ← 扩展 key（泛型、注册、默认值工厂）
│   ├── TBDamageHandler.java             ← 伤害发起方接口（含 dealDamage 便捷方法）
│   ├── TBHurtTarget.java               ← 协议伤害目标接口（简易/精密双模式）
│   └── PenetrationResult.java           ← 穿甲判定结果枚举（PENETRATED/BLOCKED/RICOCHET）
│
├── internal/                            ← 内部实现，非 API 面，不保证兼容性
│   ├── TBContextStack.java              ← ThreadLocal 栈管理（push/pop/hasContextFor）
│   └── TBExtensionKeyRegistry.java      ← 扩展 key 的全局注册表
│
├── mixin/                               ← Mixin 注入类
│   ├── EntityHurtMixin.java             ← @Mixin(Entity.class)，注入 Entity#hurt
│   └── LivingEntityHurtMixin.java       ← @Mixin(LivingEntity.class)，注入 LivingEntity#hurt
│
└── mixin_interface/                     ← Mixin 访问器接口（当前预留，暂无文件）
```

语言文件：

```
resources/assets/terminal_ballistics/lang/
  ├── en_us.json    ← 英文本地化（12 个等级 × 2 种显示名）
  └── zh_cn.json    ← 中文本地化
```

### 2.2 api/ —— 公共 API 面

这是依赖本 lib 的武器模组或护甲模组唯一需要 import 的包。`api.*` 中的所有类和接口均保证版本兼容性，语义保持稳定。各文件职责：

- **ArmorLevel**：13 级离散穿甲/护甲等级枚举（含元等级 UNPENETRABLE）。提供 RHA 值的双向映射（`fromRha`）、等级判定（`canDefeat`）、显示名（`getArmorDisplayName` / `getPenetrationDisplayName`）和边界查询（`upperRha` / `lowerRha` / `medianRha`）。是简易模式的基石——简单护甲模组甚至可以只返回一个枚举值而不关心 RHA 数值。
- **TBDamageApi**：静态方法集合，对外唯一入口。包含 `hurt(Object, TBDamageContext)`（发起协议伤害）、`hasContextFor(Object)`（重入守卫）和 `getContextFor(Object)`（上下文取回）。所有 ThreadLocal 管理逻辑封装在此类的委派调用中。
- **TBDamageContextBuilder**：Builder 模式构造器。位于 api 包但构造器为包可见，外部只能通过 `TBDamageContext.builder()` 获取实例。逐一设置各字段后调用 `build()`。
- **TBDamageContext**：不可变值对象。六个字段通过构造函数一次性赋值。新增 `getPenetrationLevel()` 便捷方法——直接从穿深值映射到 `ArmorLevel`。
- **TBDamageExtensions**：扩展数据的类型安全读写容器。内部持有一个 `Map`，对外提供带有默认值退回的 `get`/`set`。协议自身预定义 `FUSE_DELAY` 扩展 key（引信延迟）。
- **TBDamageExtensionKey**：泛型 key 类。构造器包可见，绑定 `ResourceLocation`、`Class<T>` 和默认值工厂。
- **PenetrationResult**：穿甲判定结果枚举——`PENETRATED`（击穿）、`BLOCKED`（未击穿）、`RICOCHET`（跳弹），三者互斥。由 `TBHurtTarget.resolvePenetration()` 返回，作为 `calculateFinalDamage` 和回调触发的唯一依据。
- **TBDamageHandler**：伤害发起方接口。武器/弹头模组实现，在构造上下文时注入。提供 `onPenetrated`、`onBlocked`、`onRicochet`、`onOvermatch`、`onSpall` 等回调钩子，以及 `dealDamage(target, ctx)` 便捷方法（自动将自身注入上下文再调用 API）。
- **TBHurtTarget**：核心接口。简易模式下实现者只需提供 `getArmorLevel` 和 `hurt`，其余方法均有基于离散等级的默认实现。精密模式下可覆写全部管线方法回到纯数值模型。新增 `resolvePenetration()` 方法返回 `PenetrationResult`，`calculateFinalDamage` 接收该结果作为入参。

### 2.3 internal/ —— 内部实现

此包内所有类为协议内部实现细节，不构成公开 API，不保证跨版本兼容性。

- **TBContextStack**：封装 `ThreadLocal<Deque<StackEntry>>` 的管理。`StackEntry` 是其 `private record`（target + context 对）。对外提供 `push(target, ctx)`、`pop()`、`hasContextFor(target)` 三个操作。
- **TBExtensionKeyRegistry**：全局注册表，持有已注册的 `TBDamageExtensionKey`。在 `register()` 时检查 ID 重名。

### 2.4 mixin/ —— Mixin 注入类

- **EntityHurtMixin**：`@Mixin(Entity.class)`，注入 `Entity#hurt` 的 HEAD 阶段（cancellable=true）。
- **LivingEntityHurtMixin**：`@Mixin(LivingEntity.class)`，注入点同上。必须独立存在——JVM 对 LivingEntity 实例的方法分派直接进入覆写版本。

两者对 LivingEntity 实例不会重复触发：方法分派只命中一个覆写版本。

### 2.5 依赖关系

```
            ┌─────────┐
            │  api 包  │  ← 外部 mod 仅依赖此包
            └────┬────┘
                 │ 委托
            ┌────▼────┐
            │internal包│  ← 内部实现，包可见
            └─────────┘
                 
  mixin 包 → 调用 TBDamageApi.hasContextFor() + TBDamageApi.hurt()
```

api 包中的 `TBDamageApi` 和 `TBDamageExtensions` 依赖 internal 包完成实际工作，但依赖方向始终从 api 向内，使用者的编译路径仅需 api。mixin 包引用 api，不引用 internal。

## 三、TBDamageContext —— 核心数据结构

`TBDamageContext` 是一次命中行为的完整上下文描述对象，由武器模组（协议发起方）在调用 `TBDamageApi.hurt()` 前构造。它是一个不可变的值对象——所有字段在构造时确定，穿甲判定过程中仅读取、不写入。

### 3.1 必备字段

```java
public record TBDamageContext(
    DamageSource source,              // vanilla DamageSource
    float baseDamage,                 // 标称伤害量
    Vec3 hitVelocity,                 // 命中速度矢量（世界坐标）
    Vec3 hitPoint,                    // 命中点世界坐标
    Vec3 hitNormal,                   // 命中面法线（指向面外侧）
    float penetration,                // 理论穿深（垂直RHA等效，mm），由武器侧计算
    TBDamageExtensions extensions,    // 类型安全扩展容器
    @Nullable TBDamageHandler handler // 伤害发起方回调接口，可选（null 表示无回调）
)
```

**source** —— vanilla `DamageSource` 的完整引用。协议层不创建新的 source、不修改 source、也不把额外字段塞入 source。`DamageSource#getEntity()` 返回间接攻击者，`DamageSource#getDirectEntity()` 返回直接攻击者（子弹实体）。

**baseDamage** —— 等同原版 `Entity#hurt(DamageSource, float)` 的 `amount` 参数。在穿甲判定完成后，`TBHurtTarget.calculateFinalDamage` 默认按分级逻辑处理 baseDamage：未击穿 0、同级击穿 65%、越级击穿 100%。

**hitVelocity** —— 命中瞬间弹体的速度矢量（单位 m/s）。其模长是命中速度，其方向是入射方向。需要入射方向时 `hitVelocity.normalize()` 即可。保留完整矢量而非归一化方向是因为部分装甲模型需要的正是速度大小（如动能判定）。

**hitPoint** —— 命中点的世界坐标。由武器模组的弹道追踪产生。护甲模组用它来确定命中部位，然后返回对应部位的装甲属性。

**hitNormal** —— 命中面的法线向量，指向面外侧。可用于计算入射角、播放反弹粒子等。

**penetration** —— 理论穿深。由武器模组填入，是弹体在命中条件下的 RHA 等效穿透能力（mm）。**重要：从 v2 起，角度效应等归弹头/武器侧在构造上下文前自行计算**，协议不再做默认斜穿修正。这意味着武器模组在调用 builder 时填入的 `penetration` 已经是考虑过角度、距离衰减等后的最终穿深值。协议内核只负责比较 `modifyPenetration(ctx)` 与 `getRHA(ctx)`。

**extensions** —— 类型安全扩展容器，见第五章。

**handler** —— 伤害发起方的回调接口，可选（默认 null）。武器模组通过 `builder.handler(myHandler)` 或 `ctx.withHandler(myHandler)` 注入。为 null 时管线不触发任何回调。护甲侧可在 `getRHA` 等方法内通过 `ctx.getHandler()` 查询"谁在打我"做精细判定（如爆反是否对该弹药类型生效）。

record 为不可变类型，通过 `withHandler(handler)` 返回一个新实例（原字段不变，仅替换 handler）：

```java
public TBDamageContext withHandler(TBDamageHandler handler) {
    return new TBDamageContext(source, baseDamage, hitVelocity,
        hitPoint, hitNormal, penetration, extensions, handler);
}
```

### 3.2 getPenetrationLevel()

`TBDamageContext` 新增便捷方法 `getPenetrationLevel()`，将穿深值映射到离散穿甲等级：

```java
public ArmorLevel getPenetrationLevel() {
    return ArmorLevel.fromRha(this.penetration);
}
```

该方法等价于 `ArmorLevel.fromRha(ctx.penetration())`，被 `TBHurtTarget` 的默认实现内部使用。

## 四、ArmorLevel —— 穿甲/护甲等级枚举

`ArmorLevel` 是一个 13 级枚举（12 级实体等级 + 1 级元等级），将连续 RHA 值映射为离散等级。它是简易模式的核心——参考 Helldivers 2 的离散护甲理念，**同一等级内视为防护效能相等**，不同等级之间是质变而非量变。

### 4.1 等级定义

| 枚举常量            | upperRha  | lowerRha  | 现实参照                            |
| --------------- | --------- | --------- | ------------------------------- |
| `UNARMORED_1`   | 1mm       | 0mm       | 血肉、无保护裸露表面                      |
| `UNARMORED_2`   | 3mm       | 1mm       | 几丁质甲壳、木板、薄塑料壳                   |
| `LIGHT_1`       | 5mm       | 3mm       | 轻型防弹衣、铁皮、铝合金薄板                  |
| `LIGHT_2`       | 10mm      | 5mm       | 重型防弹衣、装甲车门                      |
| `MEDIUM`        | 20mm      | 10mm      | 一般车辆车架、步战车侧后方                   |
| `HEAVY`         | 40mm      | 20mm      | 步战车正面、坦克侧后/顶部                   |
| `SUPER_HEAVY_1` | 80mm      | 40mm      | 二战早期中型坦克正面（T-34、谢尔曼）            |
| `SUPER_HEAVY_2` | 150mm     | 80mm      | 二战晚期重型坦克正面（虎王、IS-2）             |
| `SUPER_HEAVY_3` | 300mm     | 150mm     | 冷战早期主战坦克（T-55、M48）              |
| `SUPER_HEAVY_4` | 600mm     | 300mm     | 冷战中期+爆反（T-72、M60A3 ERA）         |
| `SUPER_HEAVY_5` | 1200mm    | 600mm     | 冷战晚期现代 MBT（豹2A4、M1A1 HA）        |
| `SUPER_HEAVY_6` | 2000mm    | 1200mm    | 现代 MBT 重爆反+复合间隙（挑战者2 TES、T-90M） |
| `UNPENETRABLE`  | ∞         | 2000mm    | 绝对不可击穿——元等级，仅显式引用获取             |

等级值约 ×2 增长：`1 → 3 → 5 → 10 → 20 → 40 → 80 → 150 → 300 → 600 → 1200 → 2000`。超过 2000mm 的 RHA 值仍映射到 `SUPER_HEAVY_6`——`UNPENETRABLE` 是元等级，不会由 `fromRha` 映射产生。

每个等级有三个 JSON lang key——`armor_level.{ns}.{name}.armor`（护甲显示名）和 `armor_level.{ns}.{name}.penetration`（穿甲显示名），支持中英文分别翻译。`UNPENETRABLE` 的护甲显示名为"绝对防御"（Invulnerable），穿甲显示名为"绝对穿透"（Invulnerable Pen）。

### 4.2 核心方法

```java
// 映射
public static ArmorLevel fromRha(float rha)     // RHA → 等级（向上映射），跳过UNPENETRABLE
public float upperRha()                          // 上限
public float lowerRha()                          // 下限
public float medianRha()                         // 中位值 = (lower+upper)/2

// 离散等级判定
public boolean canDefeat(ArmorLevel armorLevel)  // >= 比较，等于算击穿；对UNPENETRABLE永远返回false

// 本地化显示
public Component getArmorDisplayName()            // 护甲显示名
public Component getPenetrationDisplayName()      // 穿甲显示名
```

### 4.3 使用示例

```java
// 简单护甲模组——我只知道我是"重甲"
ArmorLevel level = ArmorLevel.HEAVY;
float rha = level.medianRha();  // → 30f（中位值），自动填入穿甲判定

// 武器模组查询自己能否打穿某个等级
ArmorLevel penLevel = ctx.getPenetrationLevel();
boolean canPen = penLevel.canDefeat(ArmorLevel.MEDIUM);  // 能打中甲？

// 绝对防御——免疫一切弹道伤害
ArmorLevel godMode = ArmorLevel.UNPENETRABLE;
boolean immune = penLevel.canDefeat(godMode);  // → false，任何武器都打不穿

// HUD 显示
player.displayClientMessage(level.getArmorDisplayName(), true);
// 中文："重甲"  英文："Heavy"
```

`UNPENETRABLE` 不会由 `fromRha` 返回，因此普通武器永远无法自然"达到"这个等级。它只能通过护甲模组在 `getArmorLevel` 中显式返回，用于表示该目标免疫一切弹道伤害。即使精密模式下覆写 `resolvePenetration`，穿深与 `getRHA` 返回的 `Float.MAX_VALUE` 比较也永远为未击穿。

## 五、TBDamageExtensions —— 类型安全扩展机制

```java
public final class TBDamageExtensions {
    private final Map<TBDamageExtensionKey<?>, Object> data;

    TBDamageExtensions() { ... }

    public <T> T get(TBDamageExtensionKey<T> key) {
        // 若未设置，返回注册时的默认值
    }

    public <T> void set(TBDamageExtensionKey<T> key, T value) { ... }
}

public final class TBDamageExtensionKey<T> {
    private final ResourceLocation id;
    private final Class<T> type;
    private final Supplier<T> defaultValueFactory;
}
```

### 5.1 注册

扩展 key 在 mod 初始化阶段通过 `TBDamageExtensions.register()` 注册为全局单例，建议存为 `public static final`。

注册时必须提供：

- **命名空间 ID**（ResourceLocation）：扩展字段的唯一标识符
- **类型**（Class\<T>）
- **默认值工厂**（Supplier\<T>）

```java
// Machine-Max 注册自己的扩展 key
public static final TBDamageExtensionKey<HitBox> HIT_BOX =
    TBDamageExtensions.register(
        ResourceLocation.fromNamespaceAndPath("machine_max", "hit_box"),
        HitBox.class, () -> null);
```

### 5.2 使用

读操作总返回非 null（未设置时退回默认值），消费者无需判空。

### 5.3 协议预定义扩展 key

| 常量           | 类型      | 默认值   | 语义                |
| ------------ | ------- | ----- | ----------------- |
| `FUSE_DELAY` | Float   | 0     | 引信延迟（s），0 表示瞬发 |
| `CALIBER`    | Float   | 0.1   | 弹体口径（m），默认 100mm 口径 |
| `MASS`       | Float   | 10    | 弹体质量（kg），默认 10kg |


## 六、TBHurtTarget —— 协议伤害目标接口

`TBHurtTarget` 是协议层的核心抽象：默认实现基于离散的 `ArmorLevel` 体系，简单模组只需实现 `getArmorLevel` 和 `hurt` 两个方法；需要精确数值判定的模组可覆写全部管线方法。

### 6.1 接口完整定义

```java
public interface TBHurtTarget {

    // ========== 简易模式：只需实现此方法 ==========

    /**
     * 返回此命中部位对应的离散护甲等级。
     * 简单模组只需实现此方法，默认的穿甲判定、伤害计算均基于此等级。
     */
    ArmorLevel getArmorLevel(TBDamageContext ctx);

    /**
     * 返回命中部位的 RHA 等效厚度（mm）。
     * 默认实现取 getArmorLevel 的中位值。精密模组应覆写。
     */
    default float getRHA(TBDamageContext ctx) {
        return getArmorLevel(ctx).medianRha();
    }

    // ========== 穿甲判定管线 ==========

    /**
     * 修正穿深。默认直接返回原始值。
     * 穿深的角度效应等应由武器侧在构造 TBDamageContext 前计算。
     * 此 hook 保留给爆反拦截、间隙衰减等护甲侧减效逻辑。
     */
    default float modifyPenetration(TBDamageContext ctx) {
        return ctx.penetration();
    }

    /**
     * 判断是否能够击穿装甲（纯击穿判定，不含跳弹）。
     * 默认基于离散等级比较（等于算击穿）。精密模组应覆写为精确 float 比较。
     */
    default boolean isArmorPenetrated(TBDamageContext ctx) {
        return ctx.getPenetrationLevel().canDefeat(getArmorLevel(ctx));
    }

    /**
     * 解析本次命中的最终穿甲结果。
     * <p>
     * 默认实现直接委托 {@link #isArmorPenetrated}，返回 PENETRATED 或 BLOCKED。
     * 需要跳弹判定的护甲模组应覆写此方法——
     * 在入射角过大时返回 RICOCHET，此时伤害流程可能被护甲侧完全跳过。
     * <p>
     * 此方法由 TBDamageApi 在管线中单次调用，其返回值作为
     * calculateFinalDamage 的入参和回调触发的唯一依据。
     *
     * @param ctx 命中上下文
     * @return 穿甲结果（PENETRATED / BLOCKED / RICOCHET，三者互斥）
     */
    default PenetrationResult resolvePenetration(TBDamageContext ctx) {
        return isArmorPenetrated(ctx) ? PenetrationResult.PENETRATED : PenetrationResult.BLOCKED;
    }

    /**
     * 根据穿甲结果计算最终伤害量。
     * <p>
     * 默认分三级（基于离散等级）：
     * <ul>
     *   <li>PENETRATED + 同级击穿（刚好击穿）：标称伤害 × 65%</li>
     *   <li>PENETRATED + 越级击穿：标称伤害 × 100%</li>
     *   <li>BLOCKED / RICOCHET：0</li>
     * </ul>
     * <p>
     * 需要非零未击穿伤害（如钝伤）、跳弹转贯穿、超匹配(碾压)加成的模组应覆写此方法。
     * 不是"未击穿 / 跳弹就不造成伤害"——护甲侧可在覆写中自由决定。
     *
     * @param ctx    命中上下文
     * @param result 由 resolvePenetration() 返回的穿甲结果
     * @return 最终伤害量
     */
    default float calculateFinalDamage(TBDamageContext ctx, PenetrationResult result) {
        if (result != PenetrationResult.PENETRATED) return 0f;
        if (ctx.getPenetrationLevel() == getArmorLevel(ctx)) return ctx.baseDamage() * 0.65f;
        return ctx.baseDamage();
    }

    // ========== 必须实现 ==========

    boolean hurt(DamageSource source, float amount);

    @Nullable
    TBDamageContext createContextFromVanilla(DamageSource source, float amount);
}
```

### 6.2 简易模式 vs 精密模式

| 模式       | 需实现                                                   | 判定方式                    | 适用场景                  |
| -------- | ----------------------------------------------------- | ----------------------- | --------------------- |
| **简易模式** | `getArmorLevel` + `hurt` + `createContextFromVanilla` | 离散等级 → 两级递进伤害（65%/100%） | 简单护甲模组、HD2 风格         |
| **精密模式** | 覆写全部管线方法                                              | 精确 float → 毫米级判定        | Machine-Max 载具、WoT 风格 |

简易模式的等价流程：

```
ctx.getPenetrationLevel().canDefeat(target.getArmorLevel(ctx))
  → true且同级 → 65%伤害
  → true且越级 → 100%伤害
  → false     → 0
```

精密模式的覆写示例（Machine-Max SubPart、含跳弹判定）：

```java
@Override public float getRHA(TBDamageContext ctx) { return hitBox.getRHA(this); }
@Override public ArmorLevel getArmorLevel(TBDamageContext ctx) { return ArmorLevel.fromRha(getRHA(ctx)); }
@Override public float modifyPenetration(TBDamageContext ctx) { return hitBox.modifyPiercing(...); }
@Override public boolean isArmorPenetrated(TBDamageContext ctx) { return modifyPenetration(ctx) > getRHA(ctx); }
@Override public PenetrationResult resolvePenetration(TBDamageContext ctx) {
    float angle = angleBetween(ctx.hitVelocity(), ctx.hitNormal());
    if (angle > 70f) return PenetrationResult.RICOCHET;  // 判定为跳弹
    return super.resolvePenetration(ctx);                  // 委托默认逻辑
}
@Override public float calculateFinalDamage(TBDamageContext ctx, PenetrationResult result) {
    return switch (result) {
        case RICOCHET -> 0f;              // 跳弹不造成伤害
        case BLOCKED -> ctx.baseDamage() * 0.1f;  // 未击穿仍有 10% 钝伤
        case PENETRATED -> /* 自定义逻辑 */;
    };
}
```

### 6.3 穿甲判定调用顺序

当 `TBDamageApi.hurt(target, ctx)` 被调用且 target 是 `TBHurtTarget` 时：

```
1. float rha = target.getRHA(ctx)              ← 简易模式：取 medianRha()
2. float modifiedPen = target.modifyPenetration(ctx) ← 简易模式：= ctx.penetration()
3. PenetrationResult result = target.resolvePenetration(ctx)   ← 简易模式：离散 canDefeat
4. float finalDmg = target.calculateFinalDamage(ctx, result) ← 简易模式：0 / 65% / 100%
5. target.hurt(source, finalDmg)
6. 如果 ctx.getHandler() != null → 触发回调（见第七章）
```

### 6.4 简易模式下对普通实体的处理

对于未实现 `TBHurtTarget` 的普通 `LivingEntity`，协议层直接调用 `living.hurt(ctx.source(), ctx.baseDamage())`，视作**完全击穿**（无护甲等级，100% 伤害）。穿深字段在此时不起作用。

## 七、PenetrationResult 与 TBDamageHandler —— 管线结果反馈

### 7.1 PenetrationResult —— 穿甲判定结果枚举

```java
/**
 * 穿甲判定的一次性结果。
 * <p>
 * PENETRATED / BLOCKED / RICOCHET 三者互斥，是命中的主结果。
 * 由 TBHurtTarget.resolvePenetration() 返回，
 * 作为 calculateFinalDamage 的入参和回调触发的唯一依据。
 * <p>
 * 超匹配(碾压)与破片不在此枚举中表达——它们由
 * TBDamageHandler.isOvermatch() / isSpall() 在回调阶段动态判定。
 */
public enum PenetrationResult {
    PENETRATED,   // 击穿（穿深足以穿透装甲）
    BLOCKED,      // 未击穿（含钝伤等，伤害量由护甲侧自由决定）
    RICOCHET      // 跳弹（穿深不足 + 入射角过大）
}
```

由 `TBHurtTarget.resolvePenetration()` 返回，作为 `calculateFinalDamage` 的入参和 `TBDamageHandler` 回调触发的唯一依据。护甲模组覆写此方法可自然地表达跳弹，不再需要手动设置 `extensions.set(RICOCHET, true)`。

`calculateFinalDamage` 接收此枚举后可自由决定伤害——BLOCKED 不一定是 0（钝伤），RICOCHET 也可能产生非零伤害（跳弹转贯穿）。

### 7.2 TBDamageHandler —— 伤害发起方回调接口

```java
/**
 * 协议伤害发起方的处理接口。
 * <p>
 * 由武器/弹头模组实现，在构造 TBDamageContext 时注入。
 * 职责：
 * <ul>
 *   <li>接收管线执行后的事件回调</li>
 *   <li>通过 isOvermatch / isSpall 的默认实现让协议层自动判定
 *       超匹配(碾压)与破片条件，实现者无需手动设置标记</li>
 *   <li>向护甲侧提供伤害来源的元信息（ctx.getHandler() 查询）</li>
 * </ul>
 */
public interface TBDamageHandler {

    // ========== 事件回调 ==========

    default void onPenetrated(TBHurtTarget target, TBDamageContext ctx) {}
    default void onBlocked(TBHurtTarget target, TBDamageContext ctx) {}
    default void onRicochet(TBHurtTarget target, TBDamageContext ctx) {}
    default void onOvermatch(TBHurtTarget target, TBDamageContext ctx) {}
    default void onSpall(TBHurtTarget target, TBDamageContext ctx) {}

    // ========== 判定方法（默认实现，可覆写） ==========

    /**
     * 判断本次命中是否为超匹配(碾压)（穿深远超装甲厚度）。
     * 默认规则：击穿 且 穿深 > 装甲厚度 × 1.5。
     */
    default boolean isOvermatch(TBHurtTarget target, TBDamageContext ctx,
                                 PenetrationResult result) {
        if (result != PenetrationResult.PENETRATED) return false;
        return target.modifyPenetration(ctx) > target.getRHA(ctx) * 1.5f;
    }

    /**
     * 判断本次命中是否产生破片（弹体碎裂）。
     * 默认规则：
     *   - 未击穿 → 弹体在表面碎裂 → 产生破片
     *   - 击穿但非超匹配(碾压) → 弹体在穿透中碎裂 → 产生破片
     *   - 超匹配(碾压) → 弹体完整穿透 → 不产生破片
     *   - 跳弹 → 弹体偏转飞走 → 不产生破片
     */
    default boolean isSpall(TBHurtTarget target, TBDamageContext ctx,
                             PenetrationResult result) {
        if (result == PenetrationResult.RICOCHET) return false;
        if (result == PenetrationResult.BLOCKED) return true;
        return !isOvermatch(target, ctx, result);
    }

    // ========== 便捷方法 ==========

    /** 发起协议伤害，并将自身注入上下文以便接收回调 */
    default float dealDamage(Object target, TBDamageContext ctx) {
        return TBDamageApi.hurt(target, ctx.withHandler(this));
    }
}
```

### 7.3 回调触发时序

在 `TBDamageApi.hurt()` 的 try 块内、管线末尾。**超匹配(碾压)与破片由 handler 的 `isOvermatch` / `isSpall` 默认方法动态判定：**

```
resolvePenetration(ctx) → result
    ↓
calculateFinalDamage(ctx, result) → finalDmg
    ↓
target.hurt(source, finalDmg)
    ↓
triggerCallbacks(handler, target, ctx, result):

  PENETRATED:
    onPenetrated()
    → isOvermatch()==true  → onOvermatch()    // 碾压，无破片
    → isSpall()==true      → onSpall()        // 击穿但碎裂

  BLOCKED:
    onBlocked()
    → isSpall()==true      → onSpall()        // 表面碎裂

  RICOCHET:
    onRicochet()                               // 仅跳弹，无超匹配(碾压)/破片
```

回调决策表：

| result | isOvermatch | isSpall | 触发的回调 |
|--------|-------------|---------|-----------|
| PENETRATED | true | false | `onPenetrated` + `onOvermatch` |
| PENETRATED | false | true | `onPenetrated` + `onSpall` |
| BLOCKED | false | true | `onBlocked` + `onSpall` |
| BLOCKED | false | false（覆写后） | `onBlocked` |
| RICOCHET | false | false | `onRicochet` |

### 7.4 两种使用路径

**路径一：简单伤害（无回调）**——直接使用 `TBDamageApi`：

```java
TBDamageContext ctx = TBDamageContext.builder()
    .source(src).baseDamage(35f).penetration(120f).build();
float dealt = TBDamageApi.hurt(target, ctx);
```

**路径二：带回调**——实现 `TBDamageHandler`，通过 `dealDamage` 自动注入：

```java
public class MyWeapon implements TBDamageHandler {
    @Override public void onRicochet(TBHurtTarget t, TBDamageContext c) {
        playRicochetSound(c.hitPoint());
    }
    @Override public void onPenetrated(TBHurtTarget t, TBDamageContext c) {
        spawnPenetrationEffects(c.hitPoint());
    }

    void fire(Entity target) {
        TBDamageContext ctx = TBDamageContext.builder()
            .source(src).baseDamage(35f).penetration(120f).build();
        float dealt = this.dealDamage(target, ctx);  // 自身注入 ctx
    }
}
```

**护甲侧利用 handler 做精细判定：**

```java
@Override
public float getRHA(TBDamageContext ctx) {
    TBDamageHandler handler = ctx.getHandler();
    if (handler instanceof ChemicalEnergyWarhead) {
        return this.eraEffectiveRha;  // 爆反对化学能弹头有效
    }
    return this.baseRha;
}
```

## 八、ThreadLocal 上下文栈与重入守卫

### 8.1 上下文栈的结构

上下文栈由 `internal.TBContextStack` 类管理，它是一个包可见的单例，封装了 `ThreadLocal<Deque<StackEntry>>`。`StackEntry` 是其内部的 `private record`，绑定 target 与 context：

```java
// TBContextStack 内部
private record StackEntry(Object target, TBDamageContext ctx) {}
```

上下文栈的元素是 **`(target, context)`** **对**，解决了重入判定的精准性问题。

### 8.2 为什么需要 target-context 对

单纯检查"栈是否为空"不足以处理嵌套伤害中的跨目标场景。典型反例：**荆棘附魔**。

```
子弹命中穿甲玩家 → TBDamageApi.hurt(player, ctx1)
  push(player, ctx1)
  
  player.hurt() 
    → 原版管线 → 荆棘触发
    → attacker.hurt(thornsSource, thornDamage)  ← 对另一个实体的伤害!
    
如果 hasContext() 只检查栈是否为空:
  → stack 非空 → "有上下文" → 不拦截 → 荆棘绕过协议 ✗

使用 hasContextFor(attacker):
  → stack.peek().target == player ≠ attacker → 拦截 ✓
```

### 8.3 TBDamageApi 的内部实现

```java
public static float hurt(Object target, TBDamageContext ctx) {
    TBDamageHandler handler = ctx.getHandler();
    TBContextStack.INSTANCE.push(target, ctx);
    try {
        if (target instanceof TBHurtTarget tb) {
            PenetrationResult result = tb.resolvePenetration(ctx);
            float finalDmg = tb.calculateFinalDamage(ctx, result);
            boolean success = finalDmg > 0f && tb.hurt(ctx.source(), finalDmg);
            float dealt = success ? finalDmg : 0f;

            if (handler != null) {
                triggerCallbacks(handler, tb, ctx, result);
            }
            return dealt;
        }
        if (target instanceof LivingEntity living) {
            return living.hurt(ctx.source(), ctx.baseDamage()) 
                ? ctx.baseDamage() : 0f;
        }
        return 0f;
    } finally {
        TBContextStack.INSTANCE.pop();
    }
}

private static void triggerCallbacks(TBDamageHandler handler, TBHurtTarget target,
                                      TBDamageContext ctx, PenetrationResult result) {
    switch (result) {
        case PENETRATED -> {
            handler.onPenetrated(target, ctx);
            if (handler.isOvermatch(target, ctx, result)) {
                handler.onOvermatch(target, ctx);
            } else if (handler.isSpall(target, ctx, result)) {
                handler.onSpall(target, ctx);
            }
        }
        case BLOCKED -> {
            handler.onBlocked(target, ctx);
            if (handler.isSpall(target, ctx, result)) {
                handler.onSpall(target, ctx);
            }
        }
        case RICOCHET -> handler.onRicochet(target, ctx);
    }
}
```

push/pop 包裹在 `try/finally` 中，确保异常也不会泄漏上下文栈。

## 九、Mixin 注入策略

### 9.1 双注入方案

必须同时注入 `Entity#hurt` 和 `LivingEntity#hurt`。仅注入其一会有遗漏：

- 只注入 `LivingEntity#hurt`：非 LivingEntity 的 TBHurtTarget 被外部伤害时不触发拦截。
- 只注入 `Entity#hurt`：`LivingEntity#hurt` 是 `Entity#hurt` 的覆写，JVM 调用 `LivingEntity` 实例的 `hurt` 时实际执行的是 `LivingEntity#hurt` 方法体，不会经过 `Entity#hurt` 的注入点。

两个 Mixin 包含相同逻辑，不会重复触发——对于引用类型为 `LivingEntity` 的实例，调用 `hurt` 只会命中 `LivingEntity#hurt` 的注入点。

### 9.2 Mixin 拦截逻辑

```java
public static void intercept(Entity self, DamageSource source, float amount,
                             CallbackInfoReturnable<Boolean> cir) {
    // 情况1：已在此目标的协议管线内 → 放行
    if (TBDamageApi.hasContextFor(self)) return;

    // 情况2：非 TBHurtTarget → 放行
    if (!(self instanceof TBHurtTarget tb)) return;

    // 情况3：协议外伤害命中 TBHurtTarget → 接管
    TBDamageContext ctx = tb.createContextFromVanilla(source, amount);
    if (ctx == null) return;

    float result = TBDamageApi.hurt(self, ctx);
    if (result > 0f) {
        cir.setReturnValue(true);
    }
}
```

## 十、武器侧角度效应处理

由于协议不再默认修正穿深，武器模组在构造 `TBDamageContext` 时必须自行完成角度效应计算。

典型公式（Machine-Max 实现）：

```java
穿透 = amount × cosθ

其中 θ 为命中速度方向与面法线的夹角。60° 入射时穿深降至 50%；掠射钳制到 1%。
```

这意味着武器模组在 builder 中填入的 penetration 已经是角度修正后的最终穿深值。协议层的 `modifyPenetration` 默认原样返回，仅作为护甲侧减效（爆反拦截、间隙衰减）的 hook。

## 十一、完整调用链示意

### 11.1 协议内伤害（武器模组 → TBHurtTarget、无回调）

```
武器模组:
  pen = amount × cosθ  （角度效应归弹头计算）
  ctx = TBDamageContext.builder()
    .source(myDamageSource).baseDamage(35f)
    .hitVelocity(vel).hitPoint(pos).hitNormal(normal)
    .penetration(pen)    // 已修正的最终穿深
    .build();
  
  TBDamageApi.hurt(tankEntity, ctx);
  
    → push(tankEntity, ctx)
    → tankEntity.resolvePenetration(ctx)
        → tankEntity.isArmorPenetrated(ctx)          // 默认委托
            → ctx.getPenetrationLevel().canDefeat(tankEntity.getArmorLevel(ctx))
            → true（同级或越级）→ PENETRATED
    → tankEntity.calculateFinalDamage(ctx, PENETRATED)
        → 同级 → baseDmg × 0.65  或  越级 → baseDmg
    → tankEntity.hurt(source, finalDmg)
        → 实现者可调用 super.hurt() 走原版，或完全自定义
    → ctx.getHandler() == null → 无回调
    → pop()
```

### 11.1b 协议内伤害（武器模组 → TBHurtTarget、带回调）

```
武器模组 (实现 TBDamageHandler):
  ctx = TBDamageContext.builder()
    .source(src).baseDamage(35f).penetration(120f).build();
  
  myHandler.dealDamage(tankEntity, ctx);
    → ctx = ctx.withHandler(myHandler)               // 自动注入
    → TBDamageApi.hurt(tankEntity, ctx)
        → push(tankEntity, ctx)
        → resolvePenetration(ctx) → PENETRATED
        → calculateFinalDamage(ctx, PENETRATED) → finalDmg
        → tankEntity.hurt(source, finalDmg)
        → triggerCallbacks: isOvermatch? → 穿深120 vs 装甲30×1.5=45 → true
        → myHandler.onPenetrated(tankEntity, ctx)     // 击穿回调
        → myHandler.onOvermatch(tankEntity, ctx)      // 超匹配(碾压)回调（无破片）
        → pop()
```

### 11.2 协议外伤害（原版攻击 → TBHurtTarget）

```
原版 or 未知模组:
  tankEntity.hurt(vanillaSource, 5.0f)
  
    → Mixin(HEAD): hasContextFor(tankEntity)? → false (栈为空)
    → tankEntity instanceof TBHurtTarget → true
    → ctx = tankEntity.createContextFromVanilla(vanillaSource, 5.0f)
        → 低信息量上下文（仅 source + baseDamage，弹道字段零值）
    → TBDamageApi.hurt(tankEntity, ctx)
        → tankEntity.resolvePenetration(ctx) → BLOCKED
        → tankEntity.calculateFinalDamage(ctx, BLOCKED) → 0
        → ctx.getHandler() == null → 无回调
    → result = 0 → cir.setReturnValue(false)
```

### 11.3 普通实体（非 TBHurtTarget）

```
武器模组:
  TBDamageApi.hurt(普通僵尸, ctx)
    → push(zombie, ctx)
    → NOT TBHurtTarget → LivingEntity → true
    → zombie.hurt(source, ctx.baseDamage())  // 直接原版，100%伤害
    → pop()
```

## 十二、边界场景说明

### 12.1 荆棘反伤

`hasContextFor(target)` 精确区分"对同一目标的嵌套调用"和"副作用产生的新伤害"。荆棘对另一实体的伤害会被正确拦截并通过 `createContextFromVanilla` 重新进入协议管线。

### 12.2 嵌套伤害（子弹→爆炸→破片→多实体）

```
TBDamageApi.hurt(tank, bulletCtx)
  push(tank, bulletCtx)
  tank 判定为击穿 → 爆炸!
    tank 内部触发了对周围3个实体的爆炸伤害:
      for each nearby entity e:
        e.hurt(explosionSource, explosionDamage)
          → Mixin: hasContextFor(e)? → peek=(tank,bulletCtx) → false
          → 每个实体独立进入协议管线 ✓
    tank.hurt(source, finalDmg) → Mixin: hasContextFor(tank)=true → 不拦截 ✓
  pop(tank, bulletCtx)
```

### 12.3 虚空/指令/创造模式杀死

这些伤害类型与穿甲语义格格不入。`createContextFromVanilla` 应返回 `null`，Mixin 直接放行走原版杀死逻辑。

### 12.4 实体作为 TBHurtTarget 目标

实体的 `Entity` 子类具有完整的原版伤害管线。当实体是 `TBHurtTarget` 时，`hurt` 可选择调用 `super.hurt()` 保留原版护甲值、保护附魔、药水等，或在调用前后添加自定义逻辑。

## 十三、实现约束与长期目标

### 13.1 实现必须遵守

- 不修改 `DamageSource` 或其子类，不通过 Mixin 向 source 注入字段
- 不依赖弱引用（WeakReference）或静态 Map 作为 side-channel
- 不绕开 vanilla hurt pipeline 直接 `setHealth`
- 不破坏 Forge/NeoForge 的事件模型和事件总线
- ThreadLocal 上下文栈在 `try/finally` 中管理，确保不会泄漏
- 不对任何 vanilla 类做字段注入（@Shadow field additions）

### 13.2 长期愿景

项目最终目标是建立一套**面向复杂弹道与物理伤害的标准化交互层**，使得：

- 简单模组可以通过几行代码获得合理的穿甲判定（HD2 风格直达）
- 精密模组可以通过覆写全部管线获得毫米级精确判定（WoT 风格直达）
- 两种风格可以在同一个服务器共存互操作
- 穿甲模型（爆反、间隙装甲等）由装甲提供方在 hook 中实现，武器方无需了解装甲内部结构
- 模组间共享的命中结果元数据通过 `TBDamageExtensions` 的标准 key 传递，生态可在不修改协议库的情况下演化
- AI/自动化系统可以统一读取上下文做出决策
- 回放与网络同步基于统一的上下文字段实现

本项目本质上应被视为：**构建于 vanilla damage pipeline 之上的高维伤害语义层**，同时为简易场景和精密场景提供两个不同抽象级别的最短路径。
