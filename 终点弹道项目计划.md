# 终点弹道伤害协议层（Terminal Ballistics Damage Protocol Layer）技术设计文档

## 一、项目定位与设计目标

本项目为 Minecraft NeoForge/Forge 模组生态定义一套"终点弹道伤害协议层（Terminal Ballistics Damage Protocol Layer）"。其核心定位是一个可供多个枪械、载具、护甲模组共同依赖的 **lib 级兼容协议**，而非一个独立运行的 damage overhaul mod。

协议的终极目标是解决以下问题：在 Minecraft 原生伤害系统中，`DamageSource` 是被设计为不可变的、registry 化的"伤害类型标签"，其职责仅限于描述伤害的种类和来源实体。对于高复杂度场景——枪炮弹道、穿甲深度、命中角度、入射法线、装甲等效厚度、模块化部件伤害等——这些信息是**一次命中过程中临时产生的高维上下文**，它们既不适合塞入 `DamageSource`（那是类型描述而非数据容器），也不适合持久存储在实体 Capability 中（它们只在该次伤害调用链内有意义）。在没有协议的情况下，不同武器模组和装甲模组只能各自发明私有的 ad-hoc 方案，互不兼容。

协议的设计原则如下：

1. **不替代原版，而是包裹原版**：协议绝不绕开 `Entity#hurt`/`LivingEntity#hurt`、绝不直接 `setHealth`、绝不重新实现完整的 combat pipeline。对于未声明协议感知的普通实体，伤害完全走原版流程，协议层透明不参与。

2. **对声明协议感知的目标接管伤害入口**：当一个实体实现了 `TBHurtTarget` 接口，它便声明了"我自行处理穿甲判定和伤害后果"。此时协议层在完成穿甲协商后直接将最终伤害值交给该目标，由目标自行决定是调用 `super.hurt()` 走原版管线，还是完全自定义处理。

3. **上下文通过 ThreadLocal 隐式传递，但 API 完全封装**：由于 vanilla 的 `hurt` 方法签名是固定的，无法显式传递额外参数，协议层使用 ThreadLocal 栈在调用链内传播上下文。但所有 push/pop/try/finally 细节完全内嵌于 `TBDamageApi.hurt()` 内部，协议使用者永远不需要直接接触 ThreadLocal。

4. **非二元设计**：穿甲不限于简单的"穿透/不穿透"。虽然默认行为是穿深 > RHA 即穿透，但 `TBHurtTarget` 接口的所有关键方法都是可覆写的 default 方法，允许高级模组实现部分穿透、多层装甲、爆炸反应装甲、间隙衰减等复杂逻辑。

5. **协议外伤害有限兼容**：原版生物攻击、TNT 爆炸、岩浆、虚空、指令等"非协议来源"的伤害，如果命中了一个 `TBHurtTarget`，协议层会自动将它转换为一个低信息量的协议上下文，然后走完整穿甲判定管线。这通过 Mixin 注入 `Entity#hurt` 和 `LivingEntity#hurt` 实现。

## 二、TBDamageContext —— 核心数据结构

`TBDamageContext` 是一次命中行为的完整上下文描述对象，由武器模组（协议发起方）在调用 `TBDamageApi.hurt()` 前构造。它是一个不可变或至少是"构造后不建议修改"的值对象——所有字段在构造时确定，穿甲判定过程中仅读取、不写入。这与其临时性和 ThreadLocal 传播模型一致：上下文只在一次调用链内有效，调用链结束后被 discard。

### 2.1 必备字段

```
public final class TBDamageContext {

    /** vanilla DamageSource，attacker、projectile、damage type 等信息均从中获取 */
    private final DamageSource source;

    /** 标称伤害，即原版 hurt(DamageSource, float amount) 中的 amount */
    private final float baseDamage;

    /** 命中速度矢量。方向向量需要时自行归一化即可 */
    private final Vec3 hitVelocity;

    /** 命中点世界坐标 */
    private final Vec3 hitPoint;

    /** 命中面法线（指向命中面外侧，即反弹方向） */
    private final Vec3 hitNormal;

    /** 理论穿深（垂直入射 RHA 等效厚度，单位 mm）。
     *  只读，整个穿甲流程中不可变。实际有效穿深由 modifyPenetration 计算 */
    private final float penetration;

    /** 类型安全扩展容器，见第三节 */
    private final TBDamageExtensions extensions;
}
```

共六个核心字段 + 一个扩展容器。没有 attacker、projectile（均由 DamageSource 提供）、没有 kineticEnergy（消费者可从 hitVelocity 与弹体质量自行计算）、没有 HitResult（hitPoint + hitNormal 已足够）、没有 material、armorSection、ricochet、spall 等（均进入 extensions）。

### 2.2 字段详解

**source** —— vanilla `DamageSource` 的完整引用。协议层不创建新的 source、不修改 source、也不把额外字段塞入 source。武器模组构造 source 的方式与普通模组完全一致。协议层仅传递它。注意 `DamageSource#getEntity()` 返回间接攻击者（如弓手），`DamageSource#getDirectEntity()` 返回直接攻击者（如箭矢/子弹实体）。对于枪械模组，通常 directEntity 是子弹实体，entity 是射击者。载具炮弹同理。

**baseDamage** —— 这等同原版 `Entity#hurt(DamageSource, float)` 的 `amount` 参数。它表示武器模组宣称的"本次命中造成的标称伤害"。在穿甲判定完成后，`TBHurtTarget` 的 `calculateFinalDamage` 默认将 baseDamage 作为击穿后的伤害（未击穿则返回 0），但可以覆写以实现更复杂的后效修正。

**hitVelocity** —— 命中瞬间弹体的速度矢量（世界坐标）。其模长是命中速度（m/s），其方向是入射方向。之前设计中的 `direction` 字段已由此完全替代：需要入射方向时，`hitVelocity.normalize()` 即可。保留完整矢量而非归一化方向的原因是部分装甲模型需要的正是速度大小（如动能判定），同时归一化方向总可从矢量导出，反之不行。

**hitPoint** —— 命中点的世界坐标。由武器模组的弹道追踪（ray trace）产生。护甲模组用它来确定命中部位（如通过碰撞箱分区），然后返回对应部位的装甲厚度。与 `hitNormal` 配合可以完全描述一次命中在空间中的位置和方向。

**hitNormal** —— 命中面的法线向量，指向面外侧。对于方块命中面，即方块面的法线；对于实体碰撞箱，由武器模组计算。入射角的计算方式为 `hitVelocity.normalize()` 与 `hitNormal` 反向的夹角。`TBHurtTarget` 的 `modifyPenetration` 默认实现用它做斜穿修正（穿深 / cosθ）。

**penetration** —— 这是武器模组填入的"理论穿深"，指该弹体在此命中条件下（距离、初速、弹种）对垂直均质装甲的 RHA 等效穿透能力，单位为毫米。注意它不是偏移入射角修正后的有效穿深——后者由 `TBHurtTarget.modifyPenetration()` 计算。penetration 在上下文中是只读的：一旦上下文构造完毕，穿深的物理意义就固定了。穿甲判定时，比较的是 `modifyPenetration(ctx)` 与 `getRHA(ctx)` 的返回值，而非直接比较 `penetration`。

## 三、TBDamageExtensions —— 类型安全扩展机制

```java
public final class TBDamageExtensions {
    // 内部存储
    private final Map<TBDamageExtensionKey<?>, Object> data;
    
    // 包内可见构造器
    TBDamageExtensions() { ... }
    
    public <T> void set(TBDamageExtensionKey<T> key, T value) { ... }
    
    public <T> T get(TBDamageExtensionKey<T> key) {
        // 若未设置，返回注册时的默认值
    }
}
```

```java
public final class TBDamageExtensionKey<T> {
    private final ResourceLocation id;
    private final Class<T> type;
    private final Supplier<T> defaultValueFactory;

    // 包内可见构造器
}
```

### 3.1 注册

扩展 key 在 mod 初始化阶段通过 `TBDamageExtensions.register()` 注册。key 本身是全局单例，建议在武器/护甲 mod 侧存为 `public static final` 常量。

注册时必须提供：
- **命名空间 ID**（ResourceLocation）：扩展字段的唯一标识符
- **类型**（Class\<T\>）：扩展数据的具体 Java 类型
- **默认值工厂**（Supplier\<T\>）：当上下文未显式设置此扩展时，`get()` 返回的默认值

```java
// 武器模组注册自己的扩展 key
public static final TBDamageExtensionKey<Float> OVERMATCH_FACTOR =
    TBDamageExtensions.register(
        ResourceLocation.fromNamespaceAndPath("example_gun", "overmatch_factor"),
        Float.class,
        () -> 1.0f
    );

// 护甲模组注册自己的扩展 key
public static final TBDamageExtensionKey<Boolean> SKIP_INTERNAL_SPALL =
    TBDamageExtensions.register(
        ResourceLocation.fromNamespaceAndPath("example_armor", "skip_spall"),
        Boolean.class,
        () -> false
    );
```

### 3.2 使用

武器模组构造上下文时通过 builder 写入扩展数据。护甲模组在 `TBHurtTarget` 的各个回调方法中从 `ctx.getExtensions()` 读取。读操作总返回非 null（因为未设置时退回到注册默认值），消费者无需判空。

协议库自身预定义少量可选的标准扩展 key，注册在 `terminal_ballistics` 命名空间下。这些 key 以 `public static final` 常量的形式定义在 `TBDamageExtensions` 类上（格式如 `TBDamageExtensions.RICOCHET`），对应的 `private static` 在类加载时通过 `register()` 完成注册。它们不是协议核心逻辑的强制依赖——武器模组可以选择性地填充它们，护甲模组可以选择性地读取它们。协议本身不依赖任何扩展 key 的语义，它们仅是便利的跨模组交流工具。具体 key 清单见 4.3 节。

## 四、TBHurtTarget —— 协议伤害目标接口

`TBHurtTarget` 是协议层的核心抽象。任何希望参与协议穿甲判定的对象都必须实现此接口。在 Minecraft 类型体系中，实现者通常分为两类：

- **LivingEntity 子类**（载具实体、生物实体、玩家等）：同时拥有原版的 `hurt(DamageSource, float): boolean` 方法和 `TBHurtTarget` 接口方法。`TBHurtTarget.hurt` 的实现可以选择简单地 `return super.hurt(source, amount)` 将伤害结果委托给原版管线（护甲、附魔、药水等），或在调用前后添加自定义逻辑。
- **非 LivingEntity 的 Entity 子类**（部分纯协议载具实体）：没有原版伤害管线，`TBHurtTarget.hurt` 必须完整实现所有伤害后果。

### 4.1 接口完整定义

```java
public interface TBHurtTarget {

    /**
     * 返回命中部位的 RHA 等效厚度。
     * 实现者根据 ctx 中的 hitPoint、hitNormal 等信息推算出命中部位，
     * 返回该部位的基础等效装甲厚度（垂直入射，不考虑角度修正）。
     * 
     * @param ctx 完整命中上下文
     * @return 基础 RHA 等效厚度（mm）
     *         返回 0 表示此处无装甲（如观察窗薄弱区、暴露乘员等），直接穿透
     *         返回 Float.MAX_VALUE 表示此处绝对无法穿透（如特殊力场）
     */
    float getRHA(TBDamageContext ctx);

    /**
     * 修正穿深。默认实现按入射角做斜穿修正：
     * effective = ctx.penetration / cos(theta)
     * 其中 theta 是 hitVelocity 与 -hitNormal 的夹角。
     * 
     * 高级装甲 mod 可覆写以支持更复杂的衰减逻辑。
     * 
     * @param ctx 完整命中上下文
     * @return 修正后的有效穿深
     */
    default float modifyPenetration(TBDamageContext ctx) {
        Vec3 vel = ctx.getHitVelocity();
        Vec3 normal = ctx.getHitNormal();
        if (vel == null || normal == null) return ctx.getPenetration();

        double velLen = vel.length();
        double normalLen = normal.length();
        if (velLen < 1e-6 || normalLen < 1e-6) return ctx.getPenetration();

        double cosTheta = Math.abs(vel.dot(normal)) / (velLen * normalLen);
        if (cosTheta < 1e-6) return Float.MAX_VALUE; // 几乎平行于表面
        return (float) (ctx.getPenetration() / cosTheta);
    }

    /**
     * 击穿判定。
     * 默认实现直接比较 modifyPenetration(ctx) 与 getRHA(ctx)。
     * 可覆写以支持非二元判定（多层装甲部分穿透、碎片穿透深度等）。
     * 
     * @param ctx 完整命中上下文
     * @return true 表示装甲被击穿
     */
    default boolean isArmorPenetrated(TBDamageContext ctx) {
        return modifyPenetration(ctx) > getRHA(ctx);
    }

    /**
     * 根据击穿结果计算最终伤害量。
     * 默认行为：击穿返回 ctx.baseDamage，未击穿返回 0。
     * 
     * 可实现部分穿透（如 30% 伤害）、超匹配加成、后效修正等。
     * 
     * @param ctx 完整命中上下文
     * @return 最终伤害量
     */
    default float calculateFinalDamage(TBDamageContext ctx) {
        return isArmorPenetrated(ctx) ? ctx.getBaseDamage() : 0f;
    }

    /**
     * 实际造成伤害。
     * 在穿甲判定和伤害计算完成后由协议层调用。
     * 
     * @param source 伤害来源
     * @param amount 最终伤害量（由 calculateFinalDamage 计算得出）
     * @return 是否成功造成伤害
     */
    boolean hurt(DamageSource source, float amount);

    /**
     * 将协议外伤害（原版攻击、爆炸等）转换为协议上下文。
     * 由 Mixin 在拦截非协议来源伤害时调用。
     * 
     * 返回 null 表示此伤害类型不应走协议管线，退回原版流程
     * （例如虚空伤害、指令伤害、创造模式杀死等）。
     * 
     * @param source 原版 DamageSource
     * @param amount 原版伤害量
     * @return 协议上下文，或 null 退回到原版
     */
    @Nullable
    TBDamageContext createContextFromVanilla(DamageSource source, float amount);
}
```

### 4.2 穿甲判定调用顺序

当 `TBDamageApi.hurt(target, ctx)` 被调用且 target 是 `TBHurtTarget` 时，协议层按以下顺序调用接口方法：

```
1. float rha = target.getRHA(ctx)
2. float modifiedPen = target.modifyPenetration(ctx)
3. boolean pierced = target.isArmorPenetrated(ctx)
     └─ 默认实现中此步骤内比较 modifiedPen > rha
4. float finalDmg = target.calculateFinalDamage(ctx)
     └─ 默认实现：pierced ? baseDamage : 0
5. target.hurt(source, finalDmg)
```

注意第 2 步和第 3 步是分离的，这意味着高级装甲模组可以覆写 `isArmorPenetrated` 实现非二元判定（例如：返回一个内部分数，配合 `calculateFinalDamage` 做比例伤害），而不影响 `modifyPenetration` 的斜穿修正语义。

对于未实现 `TBHurtTarget` 的普通 `LivingEntity`，协议层直接调用 `living.hurt(ctx.getSource(), ctx.getBaseDamage())`，视作**直接击穿**（无装甲）。穿深字段在此时不起作用。

### 4.3 协议自带默认扩展

协议库在 `TBDamageExtensions` 类上预注册以下标准扩展 key，均位于 `terminal_ballistics` 命名空间下：

| 常量 | 类型 | 默认值 | 语义 |
|------|------|--------|------|
| `TBDamageExtensions.RICOCHET` | Boolean | false | 命中是否发生了跳弹 |
| `TBDamageExtensions.SPALL` | Boolean | false | 是否产生破片 |
| `TBDamageExtensions.OVERMATCH` | Boolean | false | 是否触发超匹配 |
| `TBDamageExtensions.FUSE_DELAY` | Integer | 0 | 引信延迟（tick），0 表示瞬发 |

这些扩展并非协议核心逻辑的强制依赖。`TBHurtTarget` 的所有方法都不读取也不写入这些扩展——它们是为护甲模组与武器模组之间的**侧信道通信**保留的。例如，某个护甲模组的 `getRHA` 覆写可能设置 `ctx.getExtensions().set(TBDamageExtensions.RICOCHET, true)`，然后武器模组的音效/粒子代码在伤害返回后通过 `ctx.getExtensions().get(TBDamageExtensions.RICOCHET)` 读取它来触发跳弹音效。

## 五、ThreadLocal 上下文栈与重入守卫

### 5.1 上下文栈的结构

上下文栈由 `internal.TBContextStack` 类管理，它是一个包可见的单例，封装了 `ThreadLocal<Deque<StackEntry>>` 的全部生命周期。`StackEntry` 是其内部的 `private record`，绑定 target 与 context：

```java
// TBContextStack 内部
private record StackEntry(Object target, TBDamageContext ctx) {}

上下文栈的元素不再是单纯的 `TBDamageContext`，而是 **`(target, context)` 对**。这解决了重入判定的精准性问题。

### 5.2 为什么需要 target-context 对

单纯检查"栈是否为空"不足以处理嵌套伤害中的跨目标场景。典型反例：**荆棘附魔**。

```
子弹命中穿甲玩家 → TBDamageApi.hurt(player, ctx1)
  push(player, ctx1)
  
  player.hurt() 
    → 原版管线 → 荆棘触发
    → attacker.hurt(thornsSource, thornDamage)  ← 对另一个实体的伤害!
    
如果 hasContext() 只检查栈是否为空:
  → stack 非空 → "有上下文" → 不拦截 → 荆棘绕过协议 ✗

使用 hasContextFor(attacker):
  → stack.peek().target == player ≠ attacker → "无该目标的上下文" → 拦截 ✓
```

### 5.3 重入守卫逻辑

```java
// TBDamageApi 中的静态方法，内部委托 TBContextStack
public static boolean hasContextFor(Object target) {
    return TBContextStack.INSTANCE.hasContextFor(target);
}
```

Mixin 中的判断变为：

```java
if (TBDamageApi.hasContextFor(this)) return; // 不拦截，继续原版
// 否则：无上下文 或 上下文属于其他目标 → 视为新伤害，进入协议管线
```

这一条规则同时处理了所有嵌套伤害场景：子弹→爆炸→破片、连锁电击、荆棘反伤等。

### 5.4 TBDamageApi.hurt 的内部实现

```java
public static float hurt(Object target, TBDamageContext ctx) {
    TBContextStack.INSTANCE.push(target, ctx);
    try {
        if (target instanceof TBHurtTarget tb) {
            float finalDamage = tb.calculateFinalDamage(ctx);
            return tb.hurt(ctx.getSource(), finalDamage) ? finalDamage : 0f;
        }
        if (target instanceof LivingEntity living) {
            return living.hurt(ctx.getSource(), ctx.getBaseDamage()) 
                ? ctx.getBaseDamage() : 0f;
        }
        return 0f;
    } finally {
        TBContextStack.INSTANCE.pop();
    }
}
```

关键要义：push/pop 被包裹在 `try/finally` 中，确保无论 `hurt` 内部抛出何种异常，上下文栈都不会污染下一调用链。

## 六、Mixin 注入策略

协议层需要对 Minecraft 伤害入口方法做 Mixin 注入，以拦截"协议外伤害命中 TBHurtTarget"的情况。

### 6.1 双注入方案

必须同时注入 `Entity#hurt` 和 `LivingEntity#hurt`。仅注入其一会有遗漏：

- 只注入 `LivingEntity#hurt`：非 LivingEntity 的 TBHurtTarget（纯载具实体）被外部伤害时不触发拦截。
- 只注入 `Entity#hurt`：`LivingEntity#hurt` 是 `Entity#hurt` 的覆写，JVM 调用 `LivingEntity` 实例的 `hurt` 时实际执行的是 `LivingEntity#hurt` 方法体，不会经过 `Entity#hurt` 的注入点。

两个 Mixin 包含相同逻辑，不会重复触发——因为对于引用类型为 `LivingEntity` 的实例，调用 `hurt` 只会命中 `LivingEntity#hurt` 的注入点。

### 6.2 Mixin 拦截逻辑

```java
// EntityHurtMixin: 针对 Entity 及其非 Living 子类
@Mixin(Entity.class)
public class EntityHurtMixin {
    @Inject(method = "hurt", at = @At("HEAD"), cancellable = true)
    private void onEntityHurt(DamageSource source, float amount, 
                               CallbackInfoReturnable<Boolean> cir) {
        interceptHurt((Entity)(Object) this, source, amount, cir);
    }
}

// LivingEntityHurtMixin: 针对 LivingEntity 及其子类
@Mixin(LivingEntity.class)
public class LivingEntityHurtMixin {
    @Inject(method = "hurt", at = @At("HEAD"), cancellable = true)
    private void onLivingHurt(DamageSource source, float amount, 
                               CallbackInfoReturnable<Boolean> cir) {
        interceptHurt((Entity)(Object) this, source, amount, cir);
    }
}
```

共享的拦截判断：

```java
private static void interceptHurt(Entity self, DamageSource source, 
                                   float amount, CallbackInfoReturnable<Boolean> cir) {
    // 情况1：已经在此目标的协议管线内 → 放行（原版/self.hurt内部调用）
    if (TBDamageApi.hasContextFor(self)) return;

    // 情况2：非 TBHurtTarget → 放行
    if (!(self instanceof TBHurtTarget tb)) return;

    // 情况3：协议外伤害命中 TBHurtTarget → 接管
    TBDamageContext ctx = tb.createContextFromVanilla(source, amount);
    if (ctx == null) return; // 目标拒绝转换，退回到原版

    float result = TBDamageApi.hurt(self, ctx);
    if (result > 0f) {
        cir.setReturnValue(true);
    }
}
```

这是一个单点拦截、单点分派的模型。所有的协议伤害（无论来自协议内还是协议外）最终都归一到 `TBDamageApi.hurt()` 这一个入口。

### 6.3 事件阶段

Mixin 发生在方法入口 `HEAD`，因此是**预制**：在 vanilla 和 Forge/NeoForge 的任何 pipeline 逻辑运行之前，协议层就已经接管（或放行）。如果需要，也可以在上下文内额外触发协议专用事件（`TBArmorNegotiationEvent` 等）供护甲模组以 Forge 事件方式参与，但这些事件由 `TBDamageApi.hurt()` 内部主动发布，不需要额外的 Mixin。

## 七、完整调用链示意

### 7.1 协议内伤害（武器模组 → TBHurtTarget）

```
武器模组:
  ctx = TBDamageContext.builder()
    .source(myDamageSource)
    .baseDamage(35.0f)
    .hitVelocity(ray.getHitVelocity())
    .hitPoint(ray.getHitPos())
    .hitNormal(ray.getHitNormal())
    .penetration(120.0f)     // 120mm RHA at this range
    .extensions(exts)
    .build();
  
  TBDamageApi.hurt(tankEntity, ctx);
  
    → push(tankEntity, ctx)
    → tankEntity.calculateFinalDamage(ctx)
        → tankEntity.isArmorPenetrated(ctx)
            → tankEntity.getRHA(ctx)           // 返回命中部位 RHA
            → tankEntity.modifyPenetration(ctx) // 默认斜穿修正
            → 比较 → pierced!
        → return ctx.getBaseDamage()           // 击穿，返回 35.0
    → tankEntity.hurt(source, 35.0f)
        → (实现者选择):
        → return super.hurt(source, 35.0f)     // 走原版管线
                                              // Mixin: hasContextFor? → true → 不拦截
    → pop()
```

### 7.2 协议外伤害（原版攻击 → TBHurtTarget）

```
原版 or 未知模组:
  tankEntity.hurt(vanillaSource, 5.0f)
  
    → Mixin(HEAD): hasContextFor(tankEntity)? → false (栈为空)
    → tankEntity instanceof TBHurtTarget → true
    → ctx = tankEntity.createContextFromVanilla(vanillaSource, 5.0f)
        → 返回一个填充了 source 和 baseDamage=5.0 的 fallback ctx
    → TBDamageApi.hurt(tankEntity, ctx)
        → push(tankEntity, ctx)
        → tankEntity.calculateFinalDamage(ctx)
            → isArmorPenetrated(ctx): modifiedPen(0) vs RHA(...) 
            → 若坦克 RHA > 0 → not pierced → return 0
        → tankEntity.hurt(vanillaSource, 0)
        → pop()
    → result = 0 → cir.setReturnValue(false)  // 坦克无视了这次攻击
```

### 7.3 普通实体（非 TBHurtTarget）

```
武器模组:
  TBDamageApi.hurt(普通僵尸, ctx)
    → push(zombie, ctx)
    → zombie NOT instanceof TBHurtTarget
    → zombie instanceof LivingEntity → true
    → zombie.hurt(source, ctx.getBaseDamage())
        → 原版全程处理 ✓
    → pop()
```

普通实体完全不受协议影响，穿深等字段被忽略——等同于直接击穿。

## 八、边界场景说明

### 8.1 荆棘反伤

见第五节 5.2 的完整分析。`hasContextFor(target)` 精确地区分了"对同一目标的嵌套调用"和"副作用产生的新伤害"，确保了荆棘对另一实体的伤害会被正确识别并通过 `createContextFromVanilla` 重新进入协议管线。

### 8.2 嵌套伤害（子弹→爆炸→破片→多实体）

```
TBDamageApi.hurt(tank, bulletCtx)
  push(tank, bulletCtx)
  tank 判定为击穿 → 爆炸!
    tank 内部逻辑触发了对周围3个实体的爆炸伤害:
      for each nearby entity e:
        e.hurt(explosionSource, explosionDamage)
          → Mixin: hasContextFor(e)? peek() = (tank, bulletCtx) → false
          → e instanceof TBHurtTarget? ...
          → 每个实体独立进入协议管线 ✓
    tank.hurt(source, finalDmg) → mixed: hasContextFor(tank) = true → 不拦截 ✓
  pop(tank, bulletCtx)
```

栈中始终只有一个元素（协议的 hurt 方法在 pop 之前完成了所有下游伤害），嵌套伤害均通过 Mixin 被正确识别和处理。

### 8.3 虚空/指令/创造模式杀死

这些伤害类型的语义与穿甲格格不入。`createContextFromVanilla` 在遇到此类 DamageSource 时应返回 `null`，Mixin 将直接放行，走原版杀死逻辑。

### 8.4 玩家作为 TBHurtTarget 目标

玩家的 `LivingEntity` 子类具有完整的原版伤害管线。当玩家是 `TBHurtTarget` 时：
- 若实现者的 `hurt` 调用 `super.hurt()`：原版管线照常运行（护甲值、保护附魔、药水效果、饥饿消耗等）。
- 若实现者的 `hurt` 部分自定义：可以结合原版管线与自定义逻辑。

## 九、实现约束与长期目标

### 9.1 实现必须遵守

- 不修改 `DamageSource` 或其子类，不通过 Mixin 向 source 注入字段
- 不依赖弱引用（WeakReference）或静态 Map 作为 side-channel
- 不绕开 vanilla hurt pipeline 直接 `setHealth`
- 不破坏 Forge/NeoForge 的事件模型和事件总线
- ThreadLocal 上下文栈在 `try/finally` 中管理，确保不会泄漏
- 不对任何 vanilla 类做字段注入（@Shadow field additions）

### 9.2 长期愿景

项目最终目标是建立一套**面向复杂弹道与物理伤害的标准化交互层**，使得：

- 不同武器模组可以通过统一的穿深语义与不同护甲模组互操作
- 穿甲模型（斜穿修正、超匹配、间隙装甲等）由装甲提供方在 `modifyPenetration` 中实现，武器方无需了解装甲内部结构
- 护甲属性（RHA 等效厚度、分层结构、爆反状态等）通过 `getRHA` 封装在装甲目标内部，武器方仅需传入命中几何信息
- 模组间共享的"命中结果元数据"通过 `TBDamageExtensions` 的标准 key 传递，生态可以在不修改协议库的情况下演化
- AI/自动化系统可以统一读取上下文做出决策
- 回放与网络同步基于统一的上下文字段实现

本项目本质上应被视为：**构建于 vanilla damage pipeline 之上的高维伤害语义层**。

## 十、软件包与文件架构

### 10.1 总体布局

```
io.github.sweetzonzi.terminal_ballistics/
│
├── TerminalBallistics.java              ← @Mod 主类，入口
│
├── api/                                 ← 公共 API 面，供其他 mod 编译依赖
│   ├── TBDamageApi.java                 ← 唯一对外入口（hurt、hasContextFor）
│   ├── TBDamageContext.java             ← 上下文值对象（6 个 final 字段 + 构造器）
│   ├── TBDamageContextBuilder.java      ← TBDamageContext 的 Builder（构造器包可见，外部通过 TBDamageContext.builder() 获取）
│   ├── TBDamageExtensions.java          ← 类型安全扩展容器 + 预定义扩展 key 常量
│   ├── TBDamageExtensionKey.java        ← 扩展 key（泛型、注册、默认值工厂）
│   └── TBHurtTarget.java               ← 协议伤害目标接口（6 个方法）
│
├── internal/                            ← 内部实现，非 API 面，不保证兼容性
│   ├── TBContextStack.java              ← ThreadLocal 栈管理（push/pop/hasContextFor）
│   └── TBExtensionKeyRegistry.java      ← 扩展 key 的全局注册表
│
├── mixin/                               ← Mixin 注入类（对应的 mixins.json package）
│   ├── EntityHurtMixin.java             ← @Mixin(Entity.class)，注入 Entity#hurt
│   └── LivingEntityHurtMixin.java       ← @Mixin(LivingEntity.class)，注入 LivingEntity#hurt
│
└── mixin_interface/                     ← Mixin 访问器接口（当前预留，暂无文件）
```

### 10.2 api/ —— 公共 API 面

这是依赖本 lib 的武器模组或护甲模组唯一需要 import 的包。`api.*` 中的所有类和接口均保证版本兼容性，语义保持稳定。各文件职责：

- **TBDamageApi**：静态方法集合，对外唯一入口。包含 `hurt(Object, TBDamageContext)`（发起协议伤害）和 `hasContextFor(Object)`（仅 Mixin 内部使用，一般使用者不应调用）。所有 ThreadLocal 管理逻辑封装在此类的委派调用中，对外不可见。
- **TBDamageContextBuilder**：Builder 模式构造器。位于 api 包但构造器为包可见，外部只能通过 `TBDamageContext.builder()` 获取实例。逐一设置各字段后调用 `build()`，在 build 时验证必填字段（source 非 null 等），验证通过后调用 TBDamageContext 的 compact 构造器创建不可变实例。
- **TBDamageContext**：不可变值对象。六个 `private final` 字段通过构造函数一次性赋值。只暴露 getter，不暴露 setter。构造通过 `TBDamageContext.builder()` 获取 Builder。上下文本身不包含任何行为逻辑——它仅是数据载体。
- **TBDamageExtensions**：扩展数据的类型安全读写容器。内部持有一个 `Map`，对外提供 `get(TBDamageExtensionKey<T>)` 和 `set(TBDamageExtensionKey<T>, T)`。未设置时自动退回注册的默认值。此外，此类上以 `public static final` 常量形式直接持有协议库预定义的标准扩展 key（见 4.3 节定义），对应的 `private static` 在类加载时通过 `register()` 完成注册。
- **TBDamageExtensionKey**：泛型 key 类。构造器包可见，只能通过 `TBDamageExtensions.register()` 创建。每个 key 绑定一个 `ResourceLocation` 标识、一个 `Class<T>` 类型标记和一个 `Supplier<T>` 默认值工厂。全局唯一，建议在 mod 侧存为 `public static final`。
- **TBHurtTarget**：核心接口。包含 4 个 default 方法（getRHA / modifyPenetration / isArmorPenetrated / calculateFinalDamage）和 2 个 abstract 方法（hurt / createContextFromVanilla）。所有穿甲判定的调用顺序由 TBDamageApi 控制，接口方法不直接互相调用（default 实现彼此独立，便于单独覆写）。

### 10.3 internal/ —— 内部实现

此包内所有类为协议内部实现细节，不构成公开 API，不保证跨版本兼容性。外部 mod 不应直接引用或实例化此包中的类。当前包含：

- **TBContextStack**：封装 `ThreadLocal<Deque<StackEntry>>` 的管理。`StackEntry` 是其 `private record`（target + context 对）。对外提供 `push(target, ctx)`、`pop()`、`hasContextFor(target)` 三个操作。TBDamageApi.hurt() 内部调用 push/pop，Mixin 内部调用 hasContextFor。此类对外部完全不可见（包可见构造器）。
- **TBExtensionKeyRegistry**：全局注册表，持有已注册的 `TBDamageExtensionKey`。在 `register()` 时检查 ID 重名。`TBDamageExtensions.register()` 静态委托给此类的实例方法。

### 10.4 mixin/ —— Mixin 注入类

两个 Mixin 类，命名遵循"目标类名 + Mixin"格式（与 Machine-Max 惯例一致）。两者的注入逻辑完全相同，共享一个 private static 辅助方法。mixin 类必须列在 `src/main/resources/terminal_ballistics.mixins.json` 的 `"mixins"` 数组中（无 client-only 逻辑，无需分 client）。

- **EntityHurtMixin**：`@Mixin(Entity.class)`，在 `Entity#hurt(DamageSource, float)` 的 `@At("HEAD")` 处以 `cancellable = true` 注入。当栈中无当前目标的上下文 且 目标是 TBHurtTarget 时，接管伤害流程。
- **LivingEntityHurtMixin**：`@Mixin(LivingEntity.class)`，注入点同上。必须独立于 EntityHurtMixin 而存在——JVM 对 LivingEntity 实例的方法分派直接进入 LivingEntity#hurt 的覆写，不会触发 Entity#hurt 的注入点。

两者对 LivingEntity 实例不会重复触发：方法分派只命中一个覆写版本。

### 10.5 mixin_interface/ —— 预留

Mixin 访问器接口包。Machine-Max 中用于通过 `@Shadow` 或自定义接口向目标类添加方法（如 `machine_max$xxx`）。当前协议设计不需要向任何 vanilla 类添加接口方法——所有逻辑通过 `instanceof TBHurtTarget` 和 ThreadLocal 完成。保留空包以备未来需要（如向 Entity 添加 `tb$isProtocolTarget()` 快速判断方法等）。

### 10.6 依赖关系

```
            ┌─────────┐
            │  api 包  │  ← 外部 mod 仅依赖此包
            └────┬────┘
                 │ 委托
            ┌────▼────┐
            │internal包│  ← 内部实现，包可见
            └─────────┘
                 
  mixin 包 → 调用 TBDamageApi.hasContextFor() + TBDamageApi.hurt()
  （mixin 可以引用 api 包中的类）
```

api 包中的 `TBDamageApi` 和 `TBDamageExtensions` 依赖 internal 包完成实际工作（栈管理、注册表），但依赖方向始终从 api 向内，使用者的编译路径仅需 api。mixin 包引用 api，不引用 internal。

### 10.7 与 Machine-Max 惯例的对照

| Machine-Max | TerminalBallistics | 备注 |
|---|---|---|
| `common/` | `api/` + `internal/` | lib 的接口与实现分离，gameplay mod 无需 |
| `client/` | 无 | lib 无客户端逻辑 |
| `mixin/` | `mixin/` | 一致 |
| `mixin_interface/` | `mixin_interface/` | 一致，预留 |
| `network/` | 无（暂） | 暂无同步需求 |
| `external/` | 无 | 无外部资源 |
| `compat/` | 无 | lib 自身不包含兼容逻辑 |
| `datagen/` | 无 | lib 无游戏内容需要生成数据 |
